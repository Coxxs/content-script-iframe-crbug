{
  "manifest_version": 3,
  "name": "Content-Script Iframe Test",
  "version": "1.0",
  "description": "Minimal repro: injecting an extension iframe via content script causes microtask queues in all extension pages (popup, options, etc.) to freeze after the host page navigates",
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "web_accessible_resources": [
    {
      "resources": ["iframe.html"],
      "matches": ["<all_urls>"]
    }
  ],
  "action": {
    "default_popup": "popup.html"
  },
  "options_page": "popup.html"
}
